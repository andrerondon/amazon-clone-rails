
<div>
  <div class="topnav">
  <a class="active" href="/">Home</a>
  <a href="/contact_us">Contact</a>
  <a href="/products">Products</a>
    <div>
    <% if user_signed_in? %>
      <%= link_to "Logout", session_path, method: :delete, class: 'btn btn-danger' %>
      <%= current_user.first_name %> <%= current_user.last_name %>
    <% else %>
      <%= link_to "Log In", new_session_path, class: 'btn-btn-danger'%>
    <% end %>
  </div>
</div>


</div>